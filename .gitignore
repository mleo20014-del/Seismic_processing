# Python-generated files
__pycache__/
*.py[oc]
dist/
wheels/
*.egg-info

# Virtual environments
.venv


# Seismic data
*.h5
*.hdf5
*.segy
*.sgy
*.dat
*.npy
*.npz

output/
checkpoints/
workshot/


# System files
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/

# IDE
.idea/
*.swp

# vscode
.vscode/

# Secrets and configs
.env
config_local.py
api_keys.py

# AI
DEVELOP

# PyInstaller — артефакты сборки (НЕ исходники)
dist/
# build/ содержит build/shaders/*.qsb — они версионируются (AGENTS.md §5 MUST DO)
# Игнорируем только PyInstaller-артефакты внутри build/:
build/seismic/
build/*.pkg
build/*.exe

# *.spec — версионируется (seismic.spec нужен в репо)
# НЕ добавляем *.spec в игнор

# Compiled QSB shaders — коммитятся ТОЛЬКО рядом с изменённым .glsl в одном коммите
# build/shaders/*.qsb — НЕ игнорируем, они версионируются
*.pyc
*.pyo

# uv / pip
.uv/

# zarr / data caches
*.zarr/
*.h5
*.hdf5

# Test artifacts
.pytest_cache/
htmlcov/
.coverage

# Type stubs cache
.mypy_cache/
